# Base stage (shared)
FROM oven/bun:latest AS base
WORKDIR /app
COPY package.json ./
RUN bun install
COPY . .

# Development stage
FROM base AS dev
EXPOSE 5173
CMD ["bun", "dev"]

# Production stage
FROM base AS prod
RUN bun run build
EXPOSE 3000
CMD ["bun", "start"]
